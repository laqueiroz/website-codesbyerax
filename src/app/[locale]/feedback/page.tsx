import type { Metadata } from "next";import { FeedbackForm } from "@/components/localized/FeedbackForm";import { content,type Locale } from "@/content/localized";import { isLocale,localizedMetadata } from "@/lib/localized";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const c=content[locale].feedback;return localizedMetadata(locale,"/feedback",`${c.title} | Codes by Erax`,c.intro);}
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale:raw}=await params;const locale=raw as Locale;const c=content[locale].feedback;return <div className="document page-container narrow"><header><p className="eyebrow">Vínculo Tutoria</p><h1>{c.title}</h1><p className="lead">{c.intro}</p></header><FeedbackForm locale={locale}/></div>}
