"use client";
import Link from "next/link";import { usePathname } from "next/navigation";
export default function NotFound(){const locale=usePathname().startsWith("/en")?"en":"pt";const en=locale==="en";return <div className="document page-container narrow"><header><p className="eyebrow">404 · Codes by Erax</p><h1>{en?"This page went off course":"Esta página saiu da rota"}</h1><p className="lead">{en?"The address you visited does not exist, has changed, or is no longer available.":"O endereço acessado não existe, foi alterado ou não está mais disponível."}</p><div className="actions"><Link className="button" href={`/${locale}`}>{en?"Return Home":"Voltar ao início"}</Link><Link className="button secondary" href={`/${locale}/vinculo-tutoria`}>{en?"Discover Vínculo Tutoria":"Conhecer o Vínculo Tutoria"}</Link></div></header></div>}
